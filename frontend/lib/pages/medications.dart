import 'package:flutter/material.dart';
import '../colors/colors.dart';
import '../colors/themes.dart';

class MedicationsRunoutPage extends StatelessWidget {
  const MedicationsRunoutPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Medications Runout Page'));
  }
}
